# Python deps for embedded GPU training (training.py via libpython3 + PYTHONPATH).
# Recommended: uv venv at repo .venv-training (see scripts/training/training_uv_venv.sh).
#
#   ./scripts/training/training_uv_venv.sh --verify
#   UV_BIN=/path/to/uv ./scripts/training/training_uv_venv.sh --verify   # several uv copies on account
#   eval "$(./scripts/training/training_uv_venv.sh --export)" && zerollama serve
#
# Linux + NVIDIA (CUDA wheels via uv):
#   uv pip install --python .venv-training/bin/python -r requirements-training.txt \
#     --extra-index-url https://download.pytorch.org/whl/cu128
#
# macOS Apple Silicon (MPS; bitsandbytes excluded on Darwin):
#   uv pip install --python .venv-training/bin/python -r requirements-training.txt
#
# Verify: ./scripts/training/training_uv_venv.sh --verify

torch>=2.5
transformers>=4.40
datasets>=2.18
peft>=0.10
accelerate>=0.28
bitsandbytes>=0.43; platform_system != "Darwin"
