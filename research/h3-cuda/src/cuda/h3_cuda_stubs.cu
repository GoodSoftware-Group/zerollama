/* No remaining API stubs — int8/NAX live in h3_cuda_int8.cu (NAX = BF16 fallback). */
#include "h3_cuda_internal.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Intentionally empty: all h3_gpu_* entry points are implemented in other TUs.
 * Kept so `make` / ar still have a stubs object for historical targets. */

#ifdef __cplusplus
} /* extern "C" */
#endif
