# Wan Pure-C client → **video-c**

This tree was renamed: **`x/wan-c` → [`x/video-c`](../x/video-c/)** (`x/wan-c` remains a symlink).

Canonical operator doc: **[video-c.md](./video-c.md)**.

`wan-cli` is a symlink to `video-cli`. Prefer `ZEROLLAMA_VIDEO_CLI`; `ZEROLLAMA_WAN_CLI` still works.
